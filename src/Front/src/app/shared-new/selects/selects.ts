export enum Selects {
    select = 'select',
    booleanSelect = 'boolean-select',
    signType = 'sign-type',
    multiSelect = 'multi-select',
    selectLocal = 'select-local',
    matSelect = 'mat-select',
    matBooleanSelect = 'mat-boolean-select',
    matSignType = 'mat-sign-type',
    matMultiSelect = 'mat-multi-select',
    matSelectLocal = 'mat-select-local',
}
